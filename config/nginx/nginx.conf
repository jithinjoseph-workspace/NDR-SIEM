events {
    worker_connections 1024;
}

http {
    # NDR Engine upstream pool
    upstream ndr_engines {
        least_conn;  # Route to least busy engine
        server ndr-engine-1:3000 max_fails=3 fail_timeout=30s;
        server ndr-engine-2:3000 max_fails=3 fail_timeout=30s;
        server ndr-engine-3:3000 max_fails=3 fail_timeout=30s;
    }

    server {
        listen 80;

        # WebSocket support
        location /ws {
            proxy_pass         http://ndr_engines;
            proxy_http_version 1.1;
            proxy_set_header   Upgrade $http_upgrade;
            proxy_set_header   Connection "upgrade";
            proxy_set_header   Host $host;
            proxy_read_timeout 3600s;
        }

        # Stateful agent/sensor control routes always go to Engine 1
        location /api/start {
            proxy_pass         http://ndr-engine-1:3000;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
        }

        location /api/stop {
            proxy_pass         http://ndr-engine-1:3000;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
        }

        location /api/agent-status {
            proxy_pass         http://ndr-engine-1:3000;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
        }

        location /api/interfaces {
            proxy_pass         http://ndr-engine-1:3000;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
        }

        location /api/interface {
            proxy_pass         http://ndr-engine-1:3000;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
        }

        # API routes
        location /api {
            proxy_pass         http://ndr_engines;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
        }

        # Health check
        location /health {
            proxy_pass http://ndr_engines;
        }
    }
}